pipeline {
    agent any
    stages {
        stage('Construir rsphp'){
            steps{
                sh '/code/docker-compose down'
                sh 'docker image rm rsphp'
                sh 'docker build --tag=rsphp .'
                sh 'docker images'
            }
        }
        stage('Construir servicio rsphp + mysql'){
            steps{
                sh 'docker-compose up -d'
            }
        }
    }
}